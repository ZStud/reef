[package]
name = "reef"
version = "0.1.6"
edition = "2024"
description = "Bash compatibility layer for fish shell"
license = "MIT"

[dependencies]

[profile.release]
opt-level = "z"
lto = true
strip = true
panic = "abort"
codegen-units = 1
